{% extends 'base.html.twig' %}

{% block title %}Dialog — Demo{% endblock %}

{% block body %}
    <div class="container-sm">
        <div class="d-flex fd-column gap-5">

            {# Dialog #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>💬 Dialog</h2>
                </div>
                <div class="card-body">
                    <p>The dialog component displays important information or requires user interaction before
                        continuing. It’s often used for confirmations, forms, or alerts.</p>
                </div>
            </div>

            {# Dialog Examples #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>💠 Dialog Examples</h2>
                </div>
                <div class="card-body">
                    <div class="btn-group">
                        <button class="btn btn-tertiary" data-js="dialog" data-target="#modal">Open Dialog Simple
                        </button>
                        <button class="btn btn-tertiary" data-js="dialog" data-target="#modal-first">Open First Dialog
                        </button>
                    </div>
                </div>
            </div>

            {# How the examples are built #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>🔎 How the examples are built</h2>
                </div>

                <div class="card-body d-flex fd-column gap-1">

                    <div>
                        <h5 class="m-0">Dialog 1</h5>
                        <p class="mt-1">
                            The dialog uses the base <code class="text-bold text-primary">.dialog</code> class.<br>
                            It contains three main sections:
                            <code class="text-bold text-primary">.dialog-header</code>,
                            <code class="text-bold text-primary">.dialog-body</code>,
                            and <code class="text-bold text-primary">.dialog-footer</code>.
                        </p>
                    </div>

                    <div>
                        <h5 class="m-0">Dialog 2</h5>
                        <p class="mt-1">
                            This dialog adds <code class="text-bold text-primary">.border-y</code> on the
                            <code class="text-bold text-primary">.dialog-body</code> to visually separate its
                            content.<br>
                            The button inside the footer has
                            <code class="text-bold text-primary">.btn-primary</code> and uses
                            <code class="text-bold text-primary">data-js="dialog"</code> +
                            <code class="text-bold text-primary">data-target="#modal-second"</code> to open the next
                            dialog.</p>
                    </div>

                    <div>
                        <h5 class="m-0">Dialog 3</h5>
                        <p class="mt-1">
                            The third dialog applies <code class="text-bold text-primary">.bg-danger</code> and
                            <code class="text-bold text-primary">.border-danger</code> directly to the
                            <code class="text-bold text-primary">.dialog</code> element.<br>
                            The footer button uses <code class="text-bold text-primary">.btn-white</code> to contrast
                            with the red background.
                        </p>
                    </div>

                </div>
            </div>

            {# HTML Code #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>💻 HTML Code</h2>
                </div>
                <div class="card-body">
                    <div class="position-relative">
                        <button class="btn btn-primary position-top-right z-1 mt-1 mr-1"
                                onclick="copyCode('code-dialog', this);">Copy
                        </button>
                        <div data-js="code" id="code-dialog" class="container-code overflow-auto">
                            <!-- Button for open dialog -->
                            <button class="btn btn-primary" data-js="dialog" data-target="#modal-demo">Open Dialog
                                Simple
                            </button>
                            <!-- Dialog -->
                            <dialog class="dialog" id="modal-demo">
                                <!-- Dialog header -->
                                <div class="dialog-header">
                                    <h3>Dialog Title</h3>
                                </div>
                                <!-- Dialog body -->
                                <div class="dialog-body">
                                    <p>This is a basic dialog example. You can include any content here.</p>
                                </div>
                                <!-- Dialog footer -->
                                <div class="dialog-footer">
                                    <button class="btn btn-primary close">Close</button>
                                </div>
                            </dialog>
                        </div>
                    </div>
                </div>
            </div>

            {# Variables #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>⚙️ Variables CSS</h2>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled">
                        <li><code class="text-bold text-primary">--dialog-body-padding</code></li>
                        <li><code class="text-bold text-primary">--dialog-header-padding</code></li>
                        <li><code class="text-bold text-primary">--dialog-footer-padding</code></li>
                        <li><code class="text-bold text-primary">--dialog-backdrop-color</code></li>
                        <li><code class="text-bold text-primary">--dialog-border</code></li>
                        <li><code class="text-bold text-primary">--dialog-border-radius</code></li>
                        <li><code class="text-bold text-primary">--dialog-padding</code></li>
                        <li><code class="text-bold text-primary">--dialog-width</code></li>
                    </ul>
                </div>
            </div>

            {# Custom events #}
            <div class="card p-2 mt-3">
                <div class="card-header">
                    <h2>🔔 Events</h2>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled">
                        <li><code class="text-bold text-primary">dialog:beforeOpen</code></li>
                        <li><code class="text-bold text-primary">dialog:afterOpen</code></li>
                        <li><code class="text-bold text-primary">dialog:beforeClose</code></li>
                        <li><code class="text-bold text-primary">dialog:afterClose</code></li>
                    </ul>
                </div>
            </div>

        </div>
    </div>

    <dialog class="dialog" id="modal">
        <div class="dialog-header">
            <h3>Dialog Title</h3>
        </div>
        <div class="dialog-body">
            <p>This is a basic dialog example. You can include any content here.</p>
        </div>
        <div class="dialog-footer">
            <button class="btn close">Close</button>
        </div>
    </dialog>
    <dialog class="dialog" id="modal-first">
        <div class="dialog-header">
            <h3>First Dialog</h3>
        </div>
        <div class="dialog-body border-y">
            <p>This is the first dialog.</p>
        </div>
        <div class="dialog-footer">
            <button class="btn btn-primary close" data-js="dialog" data-target="#modal-second">Open second dialog
            </button>
        </div>
    </dialog>
    <dialog class="dialog bg-danger border-danger" id="modal-second">
        <div class="dialog-header">
            <h3>Second Dialog</h3>
        </div>
        <div class="dialog-body">
            <p>This is the second dialog.</p>
        </div>
        <div class="dialog-footer">
            <button class="btn btn-white close">Close</button>
        </div>
    </dialog>
{% endblock %}
