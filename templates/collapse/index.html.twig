{% extends 'base.html.twig' %}

{% block title %}Hello CollapseController!{% endblock %}

{% block body %}
    <div class="container-sm">
        <div class="d-flex fd-column gap-5">

            {# Intro #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>📂 Collapse</h2>
                </div>
                <div class="card-body">
                    <p>
                        A collapse works like an accordion, but with only one panel.
                        It shows or hides content when you click a trigger, keeping the interface clean and simple.
                    </p>
                </div>
            </div>

            {# Quick examples #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>💠 Examples</h2>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-12 col-6-xs">
                            <section class="collapse">
                                <button class="collapse-btn btn-primary" data-js="collapse" data-target="#collapse-1">Collapse paragraph</button>
                                <div class="collapse-body" id="collapse-1">
                                    <p class="p-2">
                                        Lorem ipsum dolor sit amet, consectetur adipisicing elit. Assumenda minima repellat
                                        reprehenderit unde? Blanditiis consequatur debitis doloremque, ea eligendi enim est hic, id
                                        inventore minima non officiis quo unde. Deserunt.
                                    </p>
                                </div>
                            </section>
                        </div>
                        <div class="col-12 col-6-xs">
                            <section class="collapse">
                                <button class="collapse-btn btn-primary" data-js="collapse" data-target="#collapse-2">Collapse list</button>
                                <div class="collapse-body" id="collapse-2">
                                    <ul class="list">
                                        <li class="list-item">menu</li>
                                        <li class="list-item">menu</li>
                                        <li class="list-item">menu</li>
                                    </ul>
                                </div>
                            </section>
                        </div>
                    </div>
                </div>
            </div>

            {# Default open state #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>📂 Default open state</h2>
                </div>
                <div class="card-body">
                    <p>
                        To have a collapse open when the page loads, add the
                        <code class="text-bold text-primary">.show</code> class to the
                        <code class="text-bold text-primary">.collapse-body</code> in your HTML.
                    </p>
                    <p class="mt-1">
                        The JavaScript then adds or removes
                        <code class="text-bold text-primary">.show</code>
                        when toggling the collapse, so the class in the markup only defines the initial state.
                    </p>
                </div>
            </div>

            {# Notes about spacing #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>📏 Spacing rules</h2>
                </div>
                <div class="card-body">
                    <p>
                        Do not set <code class="text-bold text-primary">padding</code> or
                        <code class="text-bold text-primary">margin</code> on
                        <code class="text-bold text-primary">.collapse-body</code>, as this container is reset and spacing there will not be applied as expected.
                    </p>
                    <p class="mt-1">
                        Apply spacing to elements inside the body (for example on a <code class="text-bold text-primary">p</code>,
                        <code class="text-bold text-primary">ul</code> or an inner wrapper), not on
                        <code class="text-bold text-primary">.collapse-body</code> itself.
                    </p>
                </div>
            </div>

            {# HTML Code #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>💻 HTML Code</h2>
                </div>
                <div class="card-body">
                    <div class="position-relative">
                        <button class="btn btn-primary position-top-right z-1 mt-1 mr-1"
                                onclick="copyCode('code-collapse', this);">Copy
                        </button>
                        <div data-js="code" id="code-collapse" class="container-code overflow-auto">
                            <!-- Collapse -->
                            <section class="collapse">
                                <!-- Collapse button -->
                                <button class="collapse-btn btn-primary" data-js="collapse" data-target="#collapse-demo">
                                    Collapse list
                                </button>
                                <!-- Collapse body -->
                                <div class="collapse-body" id="collapse-demo">
                                    <ul class="list">
                                        <li class="list-item">menu</li>
                                        <li class="list-item">menu</li>
                                        <li class="list-item">menu</li>
                                    </ul>
                                </div>
                            </section>
                        </div>
                    </div>
                </div>
            </div>

            {# Sizes #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>📏 Sizes</h2>
                </div>
                <div class="card-body">
                    <p>
                        Each size class (<code class="text-bold text-primary">.collapse-btn-sm</code>,
                        <code class="text-bold text-primary">.collapse-btn-md</code>,
                        <code class="text-bold text-primary">.collapse-btn-lg</code>) includes the base collapse button style with its own padding.
                    </p>
                    <div class="row mt-2">
                        <div class="col-12 col-4-xs">
                            <div class="card p-2">
                                <code class="text-bold text-primary">.collapse-btn-sm</code>
                            </div>
                        </div>
                        <div class="col-12 col-4-xs">
                            <div class="card p-2">
                                <code class="text-bold text-primary">.collapse-btn-md</code>
                            </div>
                        </div>
                        <div class="col-12 col-4-xs">
                            <div class="card p-2">
                                <code class="text-bold text-primary">.collapse-btn-lg</code>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {# Varibles #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>⚙️ Variables CSS</h2>
                </div>
                <div class="card-body">
                    <p class="p-1">
                        These variables control collapse background, borders and button padding.
                    </p>

                    <table class="table">
                        <thead>
                        <tr class="text-left">
                            <th>Variable</th>
                            <th>Description</th>
                        </tr>
                        </thead>
                        <tbody>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-bg-color</code></td>
                            <td>Background color of the <code class="text-bold text-primary">.collapse</code> container.</td>
                        </tr>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-border-radius</code></td>
                            <td>Corner radius applied to the outer <code class="text-bold text-primary">.collapse</code>.</td>
                        </tr>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-border-color</code></td>
                            <td>Border color of the collapse container.</td>
                        </tr>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-border-style</code></td>
                            <td>Border style (solid, dashed, etc.) used on the collapse.</td>
                        </tr>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-border-width</code></td>
                            <td>Border thickness of the collapse container.</td>
                        </tr>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-button-padding</code></td>
                            <td>Default padding for the <code class="text-bold text-primary">.collapse-btn</code>.</td>
                        </tr>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-button-padding-sm</code></td>
                            <td>Padding used by <code class="text-bold text-primary">.collapse-btn-sm</code>.</td>
                        </tr>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-button-padding-md</code></td>
                            <td>Padding used by <code class="text-bold text-primary">.collapse-btn-md</code>.</td>
                        </tr>
                        <tr>
                            <td><code class="text-bold text-primary">--collapse-button-padding-lg</code></td>
                            <td>Padding used by <code class="text-bold text-primary">.collapse-btn-lg</code>.</td>
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
{% endblock %}
