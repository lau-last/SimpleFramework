{% extends 'base.html.twig' %}

{% block title %}Hello GridController!{% endblock %}

{% block body %}
    <div class="container-sm">
        <div class="d-flex fd-column gap-5">

            {# Intro #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>🧩 Grid</h2>
                </div>
            </div>

            {# What is grid used for? #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>📐 What is grid used for?</h2>
                </div>

                <div class="card-body">
                    <p class="p-1">
                        The grid system provides a flexible way to create layouts using
                        the <code class="text-bold text-primary">.grid</code> container and
                        <code class="text-bold text-primary">.g-col*</code> classes.
                        It is based on 12 virtual columns and uses a built-in gap between items, controlled by
                        <code class="text-bold text-primary">--gap-grid</code>.
                    </p>
                    <p class="p-1">
                        Each <code class="text-bold text-primary">.g-col-*</code> class defines how much horizontal
                        space an item takes inside <code class="text-bold text-primary">.grid</code>. On small screens,
                        grid items stack vertically by default, and then align side by side as the viewport gets larger.
                    </p>
                </div>
            </div>

            {# Examples #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>💠 Examples</h2>
                </div>

                <div class="card-body">
                    <p class="p-1">
                        The grid uses a 12-column system, just like the column layout. Each fixed grid column class
                        (<code class="text-bold text-primary">.g-col-1</code> to
                        <code class="text-bold text-primary">.g-col-12</code>)
                        represents a fraction of the available width, minus the configured gap. Responsive variants such as
                        <code class="text-bold text-primary">.g-col-6-md</code> or
                        <code class="text-bold text-primary">.g-col-4-xs</code>
                        become active at their corresponding breakpoints.
                    </p>

                    {# Basic 2-column grid #}
                    <div class="grid">
                        <div class="g-col-12 g-col-6-md">
                            <div class="p-1 bg-primary border-radius">
                                .g-col-12 .g-col-6-md
                            </div>
                        </div>
                        <div class="g-col-12 g-col-6-md">
                            <div class="p-1 bg-primary border-radius">
                                .g-col-12 .g-col-6-md
                            </div>
                        </div>
                    </div>

                    {# 3 columns: 4/4/4 #}
                    <div class="grid">
                        <div class="g-col-12 g-col-4-md">
                            <div class="p-1 bg-primary border-radius">
                                .g-col-12 .g-col-4-md
                            </div>
                        </div>
                        <div class="g-col-12 g-col-4-md">
                            <div class="p-1 bg-primary border-radius">
                                .g-col-12 .g-col-4-md
                            </div>
                        </div>
                        <div class="g-col-12 g-col-4-md">
                            <div class="p-1 bg-primary border-radius">
                                .g-col-12 .g-col-4-md
                            </div>
                        </div>
                    </div>

                    {# Mixed layout: 3/6/3 #}
                    <div class="grid">
                        <div class="g-col-12 g-col-3-md">
                            <div class="p-1 bg-primary border-radius">
                                .g-col-12 .g-col-3-md
                            </div>
                        </div>
                        <div class="g-col-12 g-col-6-md">
                            <div class="p-1 bg-primary border-radius">
                                .g-col-12 .g-col-6-md
                            </div>
                        </div>
                        <div class="g-col-12 g-col-3-md">
                            <div class="p-1 bg-primary border-radius">
                                .g-col-12 .g-col-3-md
                            </div>
                        </div>
                    </div>

                    {# Fluid grid columns with .g-col #}
                    <div class="grid">
                        <div class="g-col">
                            <div class="p-1 bg-primary border-radius">
                                .g-col
                            </div>
                        </div>
                        <div class="g-col">
                            <div class="p-1 bg-primary border-radius">
                                .g-col
                            </div>
                        </div>
                        <div class="g-col">
                            <div class="p-1 bg-primary border-radius">
                                .g-col
                            </div>
                        </div>
                    </div>

                </div>
            </div>

            {# HTML Code #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>💻 HTML Code</h2>
                </div>

                <div class="card-body">
                    <div class="position-relative">
                        <button class="btn btn-primary position-top-right z-1 mt-1 mr-1"
                                onclick="copyCode('code-grid', this);">
                            Copy
                        </button>

                        <div data-js="code" id="code-grid" class="container-code overflow-auto">
                            <div class="grid">
                                <div class="g-col-12 g-col-6-md">Grid item 1</div>
                                <div class="g-col-12 g-col-6-md">Grid item 2</div>
                            </div>

                            <div class="grid">
                                <div class="g-col-12 g-col-4-md">Grid A</div>
                                <div class="g-col-12 g-col-4-md">Grid B</div>
                                <div class="g-col-12 g-col-4-md">Grid C</div>
                            </div>

                            <div class="grid">
                                <div class="g-col-12 g-col-3-md">Sidebar</div>
                                <div class="g-col-12 g-col-6-md">Content</div>
                                <div class="g-col-12 g-col-3-md">Sidebar</div>
                            </div>

                            <div class="grid">
                                <div class="g-col">Fluid 1</div>
                                <div class="g-col">Fluid 2</div>
                                <div class="g-col">Fluid 3</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {# Sizes / Responsive behavior #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>📏 Grid Sizes &amp; Breakpoints</h2>
                </div>

                <div class="card-body">
                    <p class="p-1">
                        The grid is based on 12 columns. You can use:
                        <code class="text-bold text-primary">.g-col</code> for fluid columns that share the available space,
                        <code class="text-bold text-primary">.g-col-{number}</code> for fixed spans of the 12-column grid,
                        and
                        <code class="text-bold text-primary">.g-col-{number}-{breakpoint}</code>
                        to change the column width from a specific breakpoint.
                        By default, any element inside
                        <code class="text-bold text-primary">.grid</code>
                        with a class starting with
                        <code class="text-bold text-primary">g-col</code>
                        is full-width on very small screens, then behaves as a grid column when its size classes become
                        active.
                    </p>

                    <div class="row mt-2">
                        <div class="col-12 col-4-xs">
                            <div class="card p-2">
                                <code class="text-bold text-primary">.g-col</code>
                                <p class="mt-1">
                                    Fluid grid column that automatically shares the available space with other
                                    <code class="text-bold text-primary">.g-col</code> siblings in the same
                                    <code class="text-bold text-primary">.grid</code>.
                                </p>
                            </div>
                        </div>

                        <div class="col-12 col-4-xs">
                            <div class="card p-2">
                                <code class="text-bold text-primary">.g-col-{number}</code>
                                <p class="mt-1">
                                    Fixed grid column width based on the 12-column system
                                    (for example:
                                    <code class="text-bold text-primary">.g-col-6</code> = 6/12 = 50%,
                                    <code class="text-bold text-primary">.g-col-4</code> = 4/12 ≈ 33.3%,
                                    with gap subtracted automatically).
                                </p>
                            </div>
                        </div>

                        <div class="col-12 col-4-xs">
                            <div class="card p-2">
                                <code class="text-bold text-primary">.g-col-{number}-{breakpoint}</code>
                                <p class="mt-1">
                                    Responsive grid column that applies its width only from the given breakpoint
                                    (for example:
                                    <code class="text-bold text-primary">.g-col-6-md</code> =
                                    full-width on small screens, 6/12 from the <code>md</code> breakpoint).
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {# Variables #}
            <div class="card p-2">
                <div class="card-header">
                    <h2>⚙️ Grid Variables</h2>
                </div>

                <div class="card-body">
                    <p class="p-1">
                        These variables control spacing and padding inside the grid system. They keep grid items aligned
                        and consistent across all breakpoints.
                    </p>

                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                            <tr class="text-left">
                                <th>Variable</th>
                                <th>Description</th>
                            </tr>
                            </thead>

                            <tbody>
                            <tr>
                                <td><code class="text-bold text-primary">--g-col-padding</code></td>
                                <td>
                                    Internal padding applied to each grid column inside
                                    <code class="text-bold text-primary">.grid</code>.
                                </td>
                            </tr>

                            <tr>
                                <td><code class="text-bold text-primary">--gap-grid</code></td>
                                <td>
                                    Base gap value used between grid items in
                                    <code class="text-bold text-primary">.grid</code>, and in the width calculations of
                                    <code class="text-bold text-primary">.g-col-{number}</code> classes.
                                </td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

        </div>
    </div>
{% endblock %}
